export interface LocationData {
  lat: number;
  lng: number;
  city: string;
  country: string;
  hostname: string;
  ip: string;
  org: string;
  postal: string;
  region: string;
  timezone: string;
}